# 拷贝这个文件为 .env，按需修改
# 注意：API_TOKEN 是 iOS 端访问后端时用的，和 GMGN_API_KEY 不是一个东西

# iOS 访问后端时要带的 Bearer Token，随便写个长字符串
API_TOKEN=dev-token-change-me-to-some-long-random-string

# GMGN_API_KEY 不在这里配置——gmgn-cli 会自己读 ~/.config/gmgn/.env
